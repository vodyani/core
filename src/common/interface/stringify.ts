/**
 * those are also defaults limits when no options object is passed into `toStringify`
 */
export interface SafeStringifyOptions {
  depthLimit: number | undefined;
  edgesLimit: number | undefined;
}
